import { BggreenDirective } from './bggreen.directive';

describe('BggreenDirective', () => {
  it('should create an instance', () => {
    const directive = new BggreenDirective();
    expect(directive).toBeTruthy();
  });
});
