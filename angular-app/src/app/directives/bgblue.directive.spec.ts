import { BgblueDirective } from './bgblue.directive';

describe('BgblueDirective', () => {
  it('should create an instance', () => {
    const directive = new BgblueDirective();
    expect(directive).toBeTruthy();
  });
});
